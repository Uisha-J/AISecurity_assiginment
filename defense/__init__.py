"""Defense module: spoof detection models (AASIST, LCNN, RawNet2).

Import submodules directly to avoid eagerly loading heavy deps:
    from voice_defense.defense.aasist import SpoofDetector   # needs transformers
    from voice_defense.defense.alt.models import LCNN, RawNet2
"""
