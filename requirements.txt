# Core
torch>=2.1.0
torchaudio>=2.1.0
numpy>=1.24
scipy>=1.10
soundfile>=0.12
librosa>=0.10
pyyaml>=6.0
tqdm>=4.65

# SSL frontend
transformers>=4.35  # WavLM, wav2vec2, XLS-R

# Eval / utils
scikit-learn>=1.3
matplotlib>=3.7
pandas>=2.0

# Augmentation
pyloudnorm>=0.1.1

# Visualization
seaborn>=0.12

# CLI
click>=8.1
rich>=13.0

# =====================================================================
# OPTIONAL — only needed for REAL model runs (heavy installs / GPU).
# The smoke tests (tests/test_smoke.py) and synthetic-data runs work
# WITHOUT any of these. Install the ones you need:
#
#   # Speaker verification (real ASR against ECAPA-TDNN):
#   pip install speechbrain>=0.5.16
#
#   # Voice cloning attack zoo:
#   pip install TTS            # Coqui XTTS v2  (primary cloner)
#   pip install openvoice      # OpenVoice
#   pip install bark           # Suno Bark
#   # RVC / seed-vc: see attack/zoo/vc/rvc.py and seed_vc.py for setup
#
#   # Post-processing / mic demo:
#   pip install pyrubberband pesq pyaudio>=0.2.14
# =====================================================================
